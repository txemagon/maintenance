#!/bin/bash

# $1: file name
file_name=$1
OUTDIR=/var/nettenance/known

while read -r line; do
    file_name=$(echo $line | cut -f1)
    echo $line | cut -f2 > "$OUTDIR/$file_name"
done < "$file_name"
